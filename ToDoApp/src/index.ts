// const paragraph = document.querySelector('p')!; // Use ! to assert that the element is not null
// console.log(paragraph.textContent); // Safe to access properties of paragraph since TypeScript knows it's not null
